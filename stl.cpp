#include<bits/stdc++.h>
using namespace std;

void print(){
     cout<< "Neelesh";
}

int sum(int a, int b)
{
    return a + b;
}
 
int main(){
    print();
    int s = sum(5, 10);
    cout << "\nSum:" << s << endl;
    return 0;
}

// C++ STL (Standard Template Library consists of ->
// 1. Containers (vector, list, deque, set, map, etc.
// 2. Algorithms (sort, reverse, find, etc.
// 3. Iterators (pointers that point to an element in a container, used to traverse the container)
// 4. Function Objects (functors, used to create objects that can be called as functions)